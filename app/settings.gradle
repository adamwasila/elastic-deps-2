rootProject.name = 'app'

includeBuild('../library1') {
    dependencySubstitution {
        substitute module('org.wasila.elasticdeps2:library1') with project(':')
    }
}

includeBuild('../library2') {
    dependencySubstitution {
        substitute module('org.wasila.elasticdeps2:library2') with project(':')
    }
}