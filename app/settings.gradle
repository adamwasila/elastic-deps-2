rootProject.name = 'app'

file("../libraries").eachDir {
    libDir ->
        if (!libDir.name.startsWith("!")) {
            includeBuild(libDir) {
                dependencySubstitution {
                    substitute module("org.wasila.elasticdeps2:${libDir.name}") with project(':')
                }
            }
        }
}

